# Core (gráficos + Excel)
matplotlib
numpy
openpyxl

# PowerPoint update
python-pptx
Pillow

# Notebook/kernel (para rodar o .ipynb via Jupyter)
ipykernel

# HTTP client (para chamar FastAPI no job fixo)
requests
